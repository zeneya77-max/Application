<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Application" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/violet_primary</item>
        <item name="colorPrimaryVariant">@color/violet_light</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!-- Standard theme for SplashActivity to avoid double splash -->
    <style name="Theme.App.Splash" parent="Theme.Application">
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="CircleImage" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <style name="OtpBoxStyle">
        <item name="android:layout_width">46dp</item>
        <item name="android:layout_height">58dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">number</item>
        <item name="android:maxLength">1</item>
        <item name="android:textColor">#1A1A1A</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/bg_otp_box</item>
    </style>
</resources>